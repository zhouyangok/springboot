<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crazyang.dao.UserMapper">
  <resultMap id="BaseResultMap" type="com.crazyang.domain.User">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="address" jdbcType="VARCHAR" property="address" />
    <result column="phone" jdbcType="VARCHAR" property="phone" />
    <result column="password" jdbcType="VARCHAR" property="password" />
  </resultMap>

  <select id="queryUserList" resultType="com.crazyang.domain.User">
    SELECT * from user where 1=1
    <if test="id != null and id.length!= 0">
      and id like CONCAT('%',#{id},'%')
    </if>
    order by id desc
  </select>
  <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
    insert into user (name, password,address,phone) values(#{name},#{password}, #{address},#{phone})
  </insert>

  <update id="update" parameterType="User">
    UPDATE user
    SET    name = #name#,
    password=#password#,
    address=#address#,
    phone=#phone#
    WHERE  id = #id#
  </update>

  <delete id="deleteById" parameterType="int">
    DELETE FROM USER
    WHERE  id = #id#
  </delete>
</mapper>